// Temporary stub — unblocks the build while static badge content is
// finalized. Renders nothing yet. Replace with the final version once
// the actual qualification badges (label/title/icon per badge) are set.
export default function BreederQualificationsGrid() {
  return null;
}
